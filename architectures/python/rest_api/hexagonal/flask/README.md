# Template

Demo implementations of a clean architectures in Python.

The goal of the "template" project is to create a simple search core for a start. The template exposes a REST API and works with three types of storage system: in-memory database, Postgres, MongoDB.


